# @razroo/iso-migrate

## 0.1.0

- Initial release.
- Adds a dependency-free TypeScript library and `iso-migrate` CLI for deterministic project migrations.
- Supports JSON pointer set/merge operations, text line insertion, exact replacement, guarded file writes, dry-run planning, idempotence checks, and config explanation.
