# Senior Agent Workflow Engineer

Example role text used by the iso-cache CLI smoke tests.

- Build deterministic agent workflow tooling.
- Keep browser automation and generated artifacts reproducible.
- Prefer local caches over repeated fetches when artifacts are safe to reuse.
