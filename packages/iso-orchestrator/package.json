{
  "name": "@razroo/iso-orchestrator",
  "version": "0.1.0",
  "description": "Durable workflow primitives for AI-agent harnesses: resumable steps, keyed mutexes, and bounded parallel fan-out with file-backed state.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "node --test --import tsx tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ci": "npm run typecheck && npm test",
    "release:check-source": "node ./scripts/release/check-source.mjs",
    "prepublishOnly": "npm run clean && npm run build && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/razroo/iso.git",
    "directory": "packages/iso-orchestrator"
  },
  "homepage": "https://github.com/razroo/iso/tree/main/packages/iso-orchestrator#readme",
  "bugs": {
    "url": "https://github.com/razroo/iso/issues"
  },
  "keywords": [
    "agent",
    "orchestrator",
    "workflow",
    "durable",
    "harness",
    "ai",
    "coding-agent",
    "claude-code",
    "cursor",
    "codex",
    "opencode"
  ],
  "publishConfig": {
    "access": "public"
  }
}
